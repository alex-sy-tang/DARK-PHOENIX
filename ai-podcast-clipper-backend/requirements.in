tqdm
torch==2.0.1
torchaudio==2.0.2
torchvision==0.15.2
opencv-python
ffmpegcv
numpy<2.0
python_speech_features
scipy
# Pinned: asd/demoTalkNet.py uses the old VideoManager-based API
# (scenedetect.video_manager), removed in v0.6 (2022-05-29). 0.5.6.1 is the
# last 0.5.x release that still has it.
scenedetect==0.5.6.1
scikit-learn
# Pinned: asd/demoTalkNet.py and asd/model/faceDetector/s3fd/__init__.py call
# `gdown --id <fileid> -O <path>` to fetch model weights. --id was deprecated
# in 4.3.1 and removed entirely in 5.0. 4.7.3 is the last 4.x release.
gdown==4.7.3
pandas
transformers
accelerate
datasets
pyarrow
google-genai
pysubs2
boto3
fastapi[standard]
whisperx>=3.8.0
